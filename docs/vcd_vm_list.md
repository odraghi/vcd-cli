```
Usage: vcd vm list [OPTIONS]

Options:
  --templates / --vms     show only templates  [default: vms]
  --status                show status
  --maintenance           show maintenance mode
  --hardware              show cpu/mem ..
  --hardware-version      show hardware version
  --network               show primary network
  --snapshot              show snapshot
  --vm-tools              show vm tools
  --guest-os              show guest os
  --sysadmin              show vsphere details
  --owner                 show owner
  --date-created          show created date
  --expired               show expired info
  --expired-notification  show expired info
  -h, --help  Show this message and exit.

```
